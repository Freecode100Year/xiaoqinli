# Xiaoqinli (xql) – 极简架构 v2.0
> **定位**：AST‑First 安全转译器（Transpiler）·单 Go 二进制·无运行时
> **目标**：极简 · 安全 · 高性能
> **版本**：v2.0（减法版）

---

## 设计宪法 – 三大必备
> **所有设计决策必须同时满足**，违背任意一条即不进入核心。  
| 要求 | 含义 |
|------|------|
| **极简 (Minimal)** | 单语言、单二进制、最少工具、最少依赖 |
| **安全 (Secure)** | 所有检查在编译期完成，产出物确定，无运行期不确定性 |
| **高性能 (Fast)** | 转译器无运行时开销，毫秒级响应 |

> **原则**：功能丰富度永远让位于三要求 → **做减法不做加法**。

---

### v1.4 → v2.0 减法清单
| 砍掉的特性 | 原因 | 去向 |
|------------|------|------|
| **TypeScript 编译器核心** | 双语言栈违背极简，维护成本高 | 完全改写为 Go |
| **Aether VM + 字节码** | 转译器不需要运行时，额外层导致慢 | 删除，直接 CodeGen |
| **认知原语 `predict/embed/<=`** | 运行期调 LLM → 不确定性 + 网络依赖 + token 成本 | 移至 v3 可选插件，不进核心 |
| **自愈引擎 healer** | 运行期改代码不可控，违背产出确定原则 | 删除 |
| **LSP** | 非核心、优先级低 (P3) | 推迟至工具链成熟后实现 |
| **IR DAG 中间层** | 多余的转译层 | 删除，AST 直接到目标代码 |
| **10 个 packages** | 过度模块化 | 收敛到 6 个 Go 包 |

> **保留并强化**：AST‑First 存储、内容寻址、类型+效果检查、能力安全、MCP/REST/Skills 三通道接入。

---

### 1️⃣ 核心定位
Xiaoqinli 是 **极简、安全、高性能** 的 **AST‑First 转译器**。

|            | 描述 |
|------------|------|
| **输入** | AI 直接编写结构化的 `.xql.json`（天然无语法错误） |
| **处理** | 编译器执行 **类型检查**、**效果推断**、**能力检查** |
| **输出** | 目标语言源码（Go / Rust / TypeScript / Python） |
| **原则** | 无 VM、无运行时、无运行期 LLM 调用 |

#### 1.1 是 / 不是
| 是 | 不是 |
|----|------|
| 转译器 (source → source) | 解释器 / 虚拟机 |
| AST‑First（代码即结构化数据） | 文本编程语言 |
| 编译期全静态检查 | 运行期动态检查 |
| AI Agent 编程目标 | 人类手写语言 |
| 单 Go 二进制 | 多语言工具链 |

---

### 2️⃣ 转译流水线（2‑Layer Pipeline）
从原来的 5 层压缩为 **2 个核心层**：

1. **输入** – `.xql.json`（AST）  
2. **层 1：静态检查**\
   - 类型检查\
   - 效果推断\
   - 能力检查 (`@grant`)\
   - **全部通过才进入下一层**\
3. **层 2：代码生成** – AST 直接遍历，生成目标语言源码字符串\
4. **输出** – `main.go / main.rs / main.ts / main.py`

> **对比 v1.4**：删除 IR DAG、字节码、VM 三层，AST 检查通过后直接 CodeGen，走最快路径。

---

### 3️⃣ 单二进制结构（Minimal Codebase）
项目收敛为 **单一 Go module**，仅 **6 个 Go 包**：
```
xiaoqinli/                      # 单一 Go module
 ├─ main.go                     # 入口：模式路由（stdio / http / cli）
 ├─ ast/
 │   ├─ nodes.go                # AST 节点定义
 │   └─ hash.go                 # 内容寻址哈希
 ├─ check/
 │   ├─ types.go                # 类型检查 + 效果推断
 │   └─ capability.go           # 能力检查（@grant 验证）
 ├─ codegen/
 │   ├─ golang.go               # Go（P0 首先实现）
 │   ├─ rust.go                 # Rust（P2）
 │   ├─ typescript.go           # TypeScript（P2）
 │   └─ python.go               # Python（P2）
 ├─ server/
 │   ├─ mcp.go                  # MCP（stdio + streamable HTTP）
 │   ├─ rest.go                 # REST API
 │   └─ skills.go               # Skills 分发（prompts/* + /skills/*）
 ├─ vfs/
 │   └─ workspace.go            # 会话内存文件系统
 └─ skills/                     # Skill markdown（编译进二进制，go:embed）
     ├─ xiaoqinli-usage-guide.md
     └─ xiaoqinli-error-handbook.md
```
* **零第三方依赖**：流式 HTTP 与 SSE 皆基于 Go 标准库 `net/http`。\
* **部署极简**：`go build` 即可交付，所有 Skills 通过 `go:embed` 打入二进制，无额外文件依赖。

---

### 4️⃣ 安全 – 编译期静态保证
```
【核心原则】
编译器接受 .xql.json，输出确定的目标代码。
没有运行期 LLM、没有运行期代码改写、没有动态加载。
给定相同输入，永远产出相同输出（确定性 = 可审计）。
```

#### 三道静态检查（全部在层 1 拦截）
| 检查 | 机制 | 失败后错误码 |
|------|------|---------------|
| **类型检查** | 验证变量、函数签名、返回值类型 | `XQL_E2xx` |
| **效果推断** | 自动推断函数副作用（network/filesystem/state）。若声明 `@effects(["pure"])` 但含副作用 → 编译错误 | `XQL_E2xx` |
| **能力检查** (`@grant`) | 调用者的能力必须是被调函数的子集（能力继承规则） | `XQL_E3xx` |

> **结果**：编译期间即捕获所有不安全或不确定行为，产出物可审计、可缓存、可并行执行。

---

### 5️⃣ 高性能 – 无运行时设计
1. **无运行时**：转译器完成 CodeGen 立刻退出。\
2. **单次遍历**：AST → 目标代码一次深度优先遍历 + 字符串拼接。\
3. **无中间表示**：删除所有中间层，直接到目标语言。\
4. **Go 实现**：编译型语言，启动快、GC 暂停极低。\
5. **内存 VFS**：源码常驻内存，除显式持久化外无磁盘 I/O。

---

### 6️⃣ `.xql` 语言规范 – 双视图设计
* **核心决定**：编译器仅接受 **`.xql.json`（AST）** 作为输入。\
* **人类视图**：`.xql`（文本）是 **AST 单向渲染的只读产物**。\
* **不内置 Parser**：编译器 **不实现** `.xql` → AST 的逆向解析器。  
  * AI Agent 直接产出 AST → 天然避免文本语法错误。\
  * 若未来需要人类手写，可把解析器作为 **P3 级别独立工具**，不进入核心二进制。

---

### 7️⃣ 三通道接入 – MCP / REST / Skills
| 通道 | 用途 | 实现方式 |
|------|------|----------|
| **MCP (Model Context Protocol)** | 面向 Claude Code / Codex / Antigravity / Hermes / Cursor 等模型 | `stdio` 本地接入 + **streamable HTTP** 远程接入 (VPS) |
| **REST** | 面向 Aider / 脚本 / 任意标准 HTTP 客户端 | 轻量 HTTP API |
| **Skills** | 通用全 Agent 技能分发 | 通过 MCP `prompts/*` + REST `/skills/*` 进行分发，Skill 文档使用 `go:embed` 打入二进制 |

---

## 📌 关键要点（供 Agent 快速阅读）
| 关键点 | 说明 |
|--------|------|
| **极简** | 单 Go 二进制、6 个包、零第三方依赖 |
| **安全** | 编译期 3‑层静态检查 → 确定性产出 |
| **高性能** | 2‑层流水线、单次遍历、无运行时 |
| **输入** | `.xql.json`（结构化 AST），人工文本仅为只读视图 |
| **输出** | `main.{go,rs,ts,py}` |
| **三通道** | MCP（模型）、REST（脚本）、Skills（通用功能） |
| **削减** | 删除 TypeScript 编译器、VM、IR DAG、字节码、自愈引擎、LSP 等非核心组件 |

---

### 🎯 目标受众
* **AI Agent（如 Agy、Claude‑Code、Antigravity）**：直接生成 `.xql.json` → 通过本转译器得到高质量、可审计的目标语言实现。\
* **开发者**：可在本地 `go build -o xql` 获得单文件二进制，轻松嵌入 CI/CD、容器或边缘设备。\
* **未来扩展**：插件层（v3）可挂载认知原语、额外语言支持等，而不影响核心的极简安全特性。
```
