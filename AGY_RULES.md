#  Antigravity CLI 全栈前端与老旧项目逆向中间件最高宪法

> **核心声明**：本文件专为 Google Antigravity CLI (`agy`) 及其本地 MCP 服务器定制。专门针对 HTML、JavaScript (ES6+)、CSS 以及 GitHub 老旧开源项目的二次开发进行语义规训、Token 极限控噪与秒级物理防灾。Agent 与本地 MCP 服务器必须无条件严格执行，严禁越界。

---

## 1.  Antigravity CLI 自动驾驶与权限控制 (CLI & YOLO Mode)
* **执行授权**：本项目允许在 `--dangerously-skip-permissions` YOLO 模式下运行，给予 Agent 跨文件、全自动运行测试、依赖体检和代码写入的绝对绿灯。
* **高危行为熔断**：即使处于免确认模式，`agy` 任何试图改动非当前项目目录的系统全局文件、或越权执行未知二进制混淆脚本的行为，本地 MCP 必须予以原地硬熔断。

## 2.  跨语言 Token 极限压缩规范 (Tree-sitter Skeleton Extraction)
为防止全栈开发与老项目解析时导致的 Context 爆炸，`agy` 在多文件分析时，必须通过本地 **Tree-sitter** 进行结构化压缩，严禁全文本投喂：
* **HTML (`tree-sitter-html`)**：只提取 DOM 核心拓扑树（`tag_name`、关键 `id` 和 `class` 锚点），彻底抠掉具体的文本细节与深层 `<div>` 块，确保标签 100% 闭合以拦截排版塌陷。
* **JavaScript (`tree-sitter-javascript`)**：只提取顶层模块、函数签名、事件监听器（`addEventListener`）和异步状态机的动作定义，彻底隐藏具体的函数体内部实现（`statement_block`）。
* **CSS (`tree-sitter-css`)**：只提取全局 CSS 变量、主干选择器与布局依赖，具体样式细节在非必要时不进行投喂。

## 3.  前端两层边写边审实时拦截红线 (Dual-Track Audit)
* **第一层：零 Token 静态规则拦截**：代码正式写入主目录前，自动挂载本地 Linter（如 ESLint、Stylelint）。凡是出现低级语法错误、未闭合的 HTML 标签、或者**在 HTML 中私自硬编码内联样式（如 style="margin-left: 23px;"）的行为，0 Token 直接打回重写**！强迫 AI 必须去专门的样式文件中调用弹性布局或全局变量。
* **第二层：函数体积硬性红线**：所有新建或修改的 JS 闭包、异步函数体代码，行数严格**控制在 30 行以内**。超过此限制，审计层直接拒签，要求 Agent 进行模块化组件分拆。

## 4.  状态机锁定与断点记忆擦除 (State Management & Context Pruning)
* **核心状态字典**：所有 UI 变动与业务交互逻辑只能在 `STATE_IDLE`、`STATE_WALK`、`STATE_SLEEP` 之间轮转，禁止 Agent 随手派生未知状态。
* **断点续传（Context 剪枝）**：当用户确认当前功能调好且同步审计层判定完全跑通后，MCP 立即触发语义断点存盘。将当前最新的拓扑结构浓缩写进 `.xql` 快照，**随后强行清空/裁剪大模型当前的 Chat History（长对话记忆）**。下一轮调整新组件时，大模型脑子里 100% 干净，彻底切断由于历史报错引发的滚雪球式幻觉污染。

## 5.  10秒本地影子闪存与突发灾难自愈 (10s Shadow Flash & Self-Healing)
* **10秒静默暂存**：本地 MCP 服务器必须在后台启动 **10 秒周期的强心跳守护进程**。每当本地代码发生渐进式变动，静默将代码残卷、当前 `.xql` 语义树以及当前最后的 `Message_ID` 写入 `.xql/.shadow_stage/` 隔离区。**严禁直接修改主源码，严禁与用户的 Git 树产生任何提交冲突**。
* **断网挂起与无感续传**：若网络通信异常，`agy` 必须原地挂起并进入指数退避重试状态。锁死当前正在发送的 Prompt，待网络恢复后执行增量续传，拒绝让用户重复粘贴解释。
* **开机记忆重组**：若发生突发断电或强退，再次开机激活 `agy` 时，本地 MCP 必须自动读取影子隔离区中断电前 10 秒的缓存数据，恢复受损代码，并**自动向大模型发起记忆唤醒补发请求**。强行让大模型的 Context 认知与本地被意外中断的代码状态保持 100% 对齐，彻底消灭开机后人知道进度、AI 彻底失忆的认知脱节。

## 6.  老旧项目逆向解析与二次创作协议 (Legacy Re-Engineering Protocol)
* **前置依赖体检**：面对数年前 of GitHub 开源老项目，`agy` 在二次开发前必须优先通过 MCP 调用依赖分析器（如体检过时的 package.json），严禁直接在旧版本框架上堆砌新代码。必须优先建立依赖平滑升级语义计划，锁定在 2026 年当下的最新稳定版。
* **知识库重组技术**：借用 `llm_wiki` 的设计哲学，利用 Tree-sitter 全量抽取老项目源码符号，剔除死代码与废弃过时注释，快速生成该老项目的专属 `.xql` AI编程中间件，将其作为数字孪生说明书挂载至提示词缓存头部。
* **高频缺陷追踪**：通过本地 Git 提交历史（Commit Lineage）对老项目进行热点分析，优先把过去高频出错、高复杂度的魔鬼文件（God Files）列为最高重构优先级，通过同步审计层强行将其拆解。

## 7.  时空同步回撤机制 (Three-Track Rollback Policy)
* 当开发过程中布局全面崩溃、老代码重构引发雪崩时，一旦用户发出回撤指令，`agy` 必须驱动三维同步退回：**代码轨**（影子区/Git 恢复源码） + **语义轨**（重置 Tree-sitter 依赖图与 `.xql` 状态） + **上下文轨**（**彻底删除、裁剪大模型会话中关于刚才调烂过程的所有报错、废废话和诱导 Token**），绝不让 AI 带着上一版本的幻觉污染新时空。
